# CheckPremissionsLocal
this script checks the premissions of local users and groups , it first asks for if its user or group then it'll asks for username or group name then for the directory or path to search then it'll show the groups that the input username is joined to and you can choose to in the member of which group you want to check the premissions .  
*this script need administrator rights to run*
